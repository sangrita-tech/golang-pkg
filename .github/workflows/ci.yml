name: CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  vuln-scan:
    uses: sangrita-tech/platform-ci/.github/workflows/scan-vulns.yml@v1.3.0
    permissions:
      contents: read
      security-events: write
