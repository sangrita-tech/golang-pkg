name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    uses: sangrita-tech/platform-ci/.github/workflows/release.yml@v1.2.0
    permissions:
      contents: write
      packages: write
